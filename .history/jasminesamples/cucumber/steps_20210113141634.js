const { Given, when, Then } = require('cucumber');
const asset = require('asset').strict;

Given('Jill Smith is not a Frequent Flyer nember', function () {});

When('She registers on the Frequent Flyer program', function () {});
