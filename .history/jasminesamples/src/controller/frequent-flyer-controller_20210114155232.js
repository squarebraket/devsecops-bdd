'use strict';
const FrequentFlyerModel = require('FrequentFlyerModel');

module.exports = function () {
  let FrequentFlyerController = () => {
    const frequentFlierModel = (new this.earnStatusPoints() = (additionalPoints) => {
      this.statusPoints = this.statusPoints + additionalPoints;
      if (this.statusPoints >= 300) {
        this.status = 'Silver';
      }
    });
  };

  return FrequentFlyerController;
};
